# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

## [1.5.3] - 2026-08-22

- 修复窗口关闭后 MCP 进程或端口可能残留的问题；退出时使用独立超时上下文完成 MCP、SSH、浏览器代理和隔离隧道清理，并增加运行结束后的兜底清理。
- MCP 启动遇到端口占用时，会核验所有权记录和进程身份；确认是 LabRemote 自身实例后强制结束并重试监听，Windows 同时兼容没有新所有权记录的旧实例。
- 终端复制粘贴改为 Ctrl+C/Ctrl+V；短时间连续两次 Ctrl+C 发送原有中断指令，右键不再触发复制或粘贴。

## [1.5.2] - 2026-08-10

- 修复长时间上传时实时速度误显示为 `0 B/s` 的根因：进度从“本地文件已读入 SFTP 发送队列”改为“远端服务器已确认写入”；
- 上传改用有界并发 `WriteAt` 管线，保留单任务最多 64 个在途 SFTP 请求的高延迟链路加速能力；
- 取消或失败时只保留服务器已连续确认的安全前缀，截断乱序尾部后可继续断点续传；
- 增加服务器确认时序、高延迟并发性能、取消与续传回归测试，并通过真实 MCP 隔离隧道大文件续传验证。

## [1.5.1] - 2026-08-10

- 修复高延迟或限速链路按稀疏批次推进时，上传实时速度在空闲归零后无法自行恢复、必须取消重传的问题；
- 新进度批次到达时使用实际批次字节与间隔立即重建速度，保留真实停滞时显示 `0 B/s` 的语义，并增加连续稀疏批次回归测试。

## [1.5.0] - 2026-08-09

- 文件传输窗口新增后台最小化：上传或下载过程中可收起为右下角任务条，继续显示状态、进度和上传实时速度，展开后保留原任务与界面状态；
- 最小化不取消、不重启传输，后台 SFTP 任务、进度事件和状态轮询保持运行；关闭和取消仍需明确确认；
- 传输窗口固定使用打开时的连接配置，最小化后切换左侧连接或终端不会把远端浏览和传输操作切换到其他服务器。

## [1.4.0] - 2026-08-09

- 上传进度新增实时速度：按累计 256 KiB 或 250 ms 任一先到进行低开销上报，后端按实际网络发送字节计算平滑速率；断点续传已存在的字节不计入速度，链路停滞 2 秒后自动显示为 `0 B/s`；
- 图形界面的上传进度卡和 MCP `file_upload_status` 同步提供 `bytes_per_second`，多文件并发时展示任务聚合上传速度；
- 更新 React 19.2、xterm 6、Vite 8、Vitest 4、PostCSS、MCP Go SDK、SFTP 与 Go 网络/加密依赖，并完成相应兼容迁移。

## [1.3.0] - 2026-08-09

- 增加密码保护的 `.lrcx` 连接包导入导出：支持选择多个连接，并携带配置、隧道/SSH 凭据、SSH 私钥内容与口令、服务器证书和主机信任；
- 连接包使用 Argon2id 派生密钥和 AES-256-GCM 认证加密，不包含全局 MCP Token；错误密码或被篡改的文件会拒绝导入；
- 导入连接始终生成新 ID，重名安全改名且不覆盖现有配置；私钥复制到 LabRemote 配置目录，失败时回滚本次新增的配置、凭据、信任和私钥文件。

## [1.2.2] - 2026-08-05

- 连接配置工具栏和右键菜单增加“复制”功能：副本自动清空连接名称并生成新的内部身份，服务器、分组、连接方式、用户名、MCP 权限等可编辑配置保持不变；
- 复制保存时从操作系统安全凭据库安全复制隧道密码、SSH 密码或私钥路径与口令，用户在副本中填写的新凭据会覆盖来源；来源连接及其凭据不会被修改；
- 相同端点的副本保留已固定的隧道证书与 SSH 主机指纹；修改端点后不复用旧信任，仍需按首次连接流程核验新指纹。

## [1.2.1] - 2026-08-05

- 大文件上传改为按剩余字节数和并行文件数自适应使用 1-64 个标准 SFTP 写请求，单任务总在途请求保持不超过 64 个（约 2 MiB 数据窗口），显著改善高延迟链路吞吐；
- 上传进度按 256 KiB 批量汇报，减少每个 32 KiB 数据包触发的锁竞争和界面事件，同时保持取消响应与断点续传安全语义；
- 增加真实 SFTP 延迟链路性能回归测试：8 MiB 数据下，旧固定 8 请求耗时约 484 ms，新自适应 64 请求约 71 ms，并校验上传内容 SHA-256 一致。

## [1.2.0] - 2026-07-26

- MCP 增加异步文件下载：可按 `profile_id` 选择服务器并把指定远端文件或目录下载到本机已存在的绝对目录；复用并发 SFTP、安全分片和断点续传能力；
- MCP 文件下载使用独立的 Profile 最小权限，旧配置默认不授权；远端路径和本机目标目录必须明确提供，默认拒绝覆盖；
- MCP 只能查询或取消当前服务实例自己创建的下载任务；关闭服务或重新生成 Token 会取消 MCP 自有上传和下载，不影响图形界面传输；
- `profiles_list` 返回独立下载权限，动态 AI 操作手册和静态文档更新为 16 个工具，并加入完整下载流程、状态与错误说明。

## [1.1.0] - 2026-07-23

- MCP 增加异步文件上传：可按 `profile_id` 选择服务器并把本机文件或目录上传到指定远端目录；复用并发 SFTP、安全分片和断点续传能力；
- MCP 文件上传使用独立的 Profile 最小权限，旧配置默认不授权；本地路径必须是绝对路径，目标目录必填，默认拒绝覆盖；
- MCP 只能查询或取消当前服务实例自己创建的上传任务；关闭服务或重新生成 Token 会取消 MCP 自有上传，不影响图形界面传输；
- `profiles_list` 返回文件上传权限，AI 操作手册和静态 MCP 文档更新为 13 个工具；
- 增加“仅 SSH（直接连接）”模式，不要求 SoftEther/VPN 配置；旧连接默认保持隔离隧道模式；
- 仅 SSH 模式下的终端、SFTP 和 MCP 直接使用 SSH，网页访问仍通过 SSH `direct-tcpip` 转发，可访问远端 `127.0.0.1` 或内网端口而无需公网开放；
- 网页访问会在 SSH 或可选隔离隧道意外断开后自动恢复连接并重试一次，现有浏览器代理地址无需重新创建；
- SSH 增加密码和私钥文件两种认证方式；私钥文件路径与可选口令保存在系统安全凭据库，Profile JSON 不记录本地路径；
- 删除、强制断开、指纹确认、凭据清理和传输取消等确认操作改用 LabRemote 应用内弹窗，不再显示 WebView 的 `localhost` 原生提示；
- 使用 MIT License 开源；
- 增加 Linux amd64 与 macOS Universal 发行包；
- macOS 使用 Keychain、Linux 使用 Secret Service 保存凭据；
- 手动或标签触发的多平台构建会自动汇总到 GitHub Release。

## [1.0.0] - 2026-07-19

- 首次公开发布；
- 提供进程内 SoftEther 隔离隧道、SSH 多标签终端和网页访问；
- 提供支持拖拽、并发与断点续传的上传/下载；
- 提供带最小权限、Bearer Token、审计和导出说明手册的本地 MCP 服务；
- 提供 Windows amd64 可执行文件与 NSIS 用户级安装包构建。
